[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "klara-greenhouse"
version = "1.0.0"
description = "Greenhouse intelligence for Atlantic Canada"
requires-python = ">=3.11"
